テーマ3着手以降の目標：テーマ3の開発

0. [Obsolete] モニタのデバッグ作業締切目安：1/6
	それでも残り3日で共同開発・個人開発を行うのは厳しい

1. 共同開発部
・[Obsolete] テキストp.41よりUART2を有効にしてシリアルの2ポート化
・シリアルポート番号をファイルディスクリプタ(fd)として使用する。
　inbyte(), outbyte(), csys68k.c のfd対応が必要
　csys68k.c (read(), write()) は基本的にはfd対応だが、
　　inbyte(), outbyte() の呼び出しがfd対応になっていないだけ。

・テキストp.41-43よりシリアルfdにストリームを割り当てる
	fscanf(), fprintf() でシリアルポート指定ができるようになる。

・P.43 に修正
  旧: #include <varargs.h>
  新: #include <stdarg.h>

[注意]
fcntl() は ... を省略せず、
#include <stdarg.h>
int fcntl(int fd, int cmd, ...){
	
}
として作成すること。
", ..." は stdarg.h により関数引数の個数が可変であることを意味する。

2. 個人開発部 (注意：共同開発ではない)
・これまで作成したマルチタスク環境を用いたアプリケーションの自由製作
・内容が十分であれば、必ずしも2ポートの利用は必須ではない。

1/9 追加：
/home/share/SW2/mtk/mon_r2.tar.gz の中にある mon_r2.o は2ポート対応済みのモニタ。
 
/home/share/SW2/mtk/test1_escseq.c
　　エスケープシーケンスをいくつか試すためのプログラム。これ自体はマルチタスクプログラムサンプルではない。
　　test1 の実機上動作試験プログラムとしても、
　　実機上ではなくPCの端末から動作させるプログラムとしても機能する。
　　エスケープシーケンスが解釈されるか否かは、端末プログラム及び端末エミュレータがサポートしているかによる。
